export default function Input({type, classname, value, id, name, onChange, placeholder, autoComplete, autoFocus }) {
    return (
        <input type="text"
        className=""
        value={}
        id=""
        name=""
        onChange={}
        placeholder=""
        autoComplete=""
        autoFocus
         />
    )
}